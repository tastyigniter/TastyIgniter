name: CI Pipeline

on:
  push:
    branches:
      - 4.x
  workflow_dispatch:

jobs:
  php-tests:
    strategy:
      matrix:
        php: [ '8.3', '8.4', '8.5' ]
    uses: tastyigniter/workflows/.github/workflows/php-tests.yml@main
    with:
      php-version: ${{ matrix.php }}
