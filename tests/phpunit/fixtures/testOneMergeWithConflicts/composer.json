{
    "repositories": [
        {
            "type": "vcs",
            "url": "./.."
        }
    ],
    "require": {
        "wikimedia/composer-merge-plugin": "*@dev",
        "monolog/monolog": "~1.0"
    },
    "require-dev": {
        "foo": "1.0.0"
    },
    "extra": {
        "merge-plugin": {
            "include": [
                "composer.local.json"
            ]
        }
    }
}
